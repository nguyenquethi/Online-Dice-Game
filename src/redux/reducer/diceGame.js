const initialState = {
    yourChoosing: true,
    goal: 0,
    totalGame: 0,
    listDice: [{dice1: 1}, {dice2: 3}, {dice3: 4}],
};

export const diceGame = (state = initialState, action) => {
    return state;
}